require 'spec_helper'

describe Rake::Stretcher do
  it 'has a version number' do
    expect(Rake::Stretcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
